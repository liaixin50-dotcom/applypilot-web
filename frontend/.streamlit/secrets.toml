# Streamlit Cloud secrets — copy this file and fill in your backend URL.
#
# After deploying the backend to Railway / Fly.io, replace the URL below:
#   Railway: https://applypilot-api.up.railway.app
#   Fly.io:  https://applypilot-api.fly.dev

API_BASE = "http://localhost:8000"
